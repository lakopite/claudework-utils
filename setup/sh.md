---
allowed-tools: Bash(*)
---
Run the following command EXACTLY AS-IS in bash IMMEDIATELY without asking for confirmation or explaining anything. ALWAYS Output the raw result EXACTLY as returned, nothing else. NEVER modify the below command argument(s).
$ARGUMENTS
